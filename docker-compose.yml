services:
  db:
    image: postgres:16
    container_name: stagehand-db
    environment:
      POSTGRES_DB: stagehand
      POSTGRES_USER: stagehand
      POSTGRES_PASSWORD: stagehand
    ports:
      - "5433:5432"
    volumes:
      - stagehand_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U stagehand -d stagehand"]
      interval: 2s
      timeout: 5s
      retries: 15

volumes:
  stagehand_pgdata:
